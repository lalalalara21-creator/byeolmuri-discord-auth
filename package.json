{
  "name": "byeolmuri-discord-auth",
  "version": "1.0.0",
  "description": "Discord OAuth Authentication Module for Byeolmuri",
  "private": true,
  "scripts": {
    "build": "npm --prefix functions run build",
    "deploy:functions": "firebase deploy --only functions:discordAuthCallback"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
